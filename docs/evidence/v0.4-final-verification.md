# v0.4.0 final verification

## Clean release gate

On 2026-07-14 (Asia/Jerusalem), verification began with `npm.cmd ci --no-audit --no-fund`, installing 299 exact locked packages. The first fail-fast pass found four formatting differences and the package smoke correctly rejected the not-yet-added v0.4 release record. After formatting and adding that record, the complete gate passed:

- format, ESLint, strict TypeScript, and production ESM build;
- 91 unit tests in 15 files and 12 integration tests in 6 files;
- installed Codex protocol check: `0.144.0-alpha.4`, 1,008 generated files, digest `d27d0610897f1e9bc4d344eca4ce89628d5215495c2d3e3cc19dedba00cf6961`;
- coverage across all 103 tests: 72.64% statements, 68.31% branches, 80.48% functions, and 76.01% lines;
- repository and official plugin-creator validation;
- plugin production-runtime build, forbidden live-state/absolute-user-path scan, and clean-copy smoke from a Unicode path with spaces;
- all 13 plugin tools discovered, `list_agents` called, and an unknown trusted caller rejected before host startup;
- clean-installed package artifact `codex-inter-agent-messaging-0.4.0.tgz` with all three entrypoints and the v0.4 release record.

Version synchronization was mechanically checked across root package/lock, plugin manifest, plugin runtime package/lock, central source/runtime metadata, release record, bridge owner protocol 2, signed descriptor schema 3, and database schema 6.

## Real runtime acceptance

The pinned stock CLI remote path started one owner, resumed/registered existing test history `019f6082-fd66-7da2-aa9f-b6461c2c486d`, passed the child-only bearer environment contract, and exercised a remote archive request against an intentionally nonexistent ID without mutating a real history.

Automatic topology exchanges completed:

- synchronous message `msg_b898c0de-9edb-47f8-95c1-1df59024986f`, turn `019f6238-ae9f-7643-be08-8c022abb4f26`, final marker `MVP_ACCEPTANCE_OK`;
- async message `msg_5aec0737-c292-40bf-b069-60c6ab47213a`, turn `019f623b-807d-7560-a66c-1616a0b56fc2`;
- group message `gmsg_3a2a7a34-3226-4c41-8fd1-f3c64907a4d1`, delivery `msg_b30af303-1741-46bd-855d-8847fbcadc81`.

The final bounded stability harness performed three clean starts with ownership generations `7957ffd6-d143-4ce4-a573-46c80ce66a53`, `7aa0e3ea-1410-4915-a37a-def65c4c5c49`, and `66588336-0bc3-41ab-bbcd-79e749b1f90a`; three simultaneous MCP processes converged on generation `71ef0340-f265-4fb1-936e-c79d435ab40c`; supervisor crash recovered through `stale-descriptor-removed`; and an unbound real-runtime recipient returned `UNSUPPORTED_THREAD_OWNER` before `turn/start`.

## Cleanup and scope

After every final scenario, authenticated stop removed the descriptor/lock and terminated the recorded supervisor/app-server PIDs. No stability, plugin, owner, or real-exchange temporary directory remained; no default-data descriptor remained; and the development plugin cache entry and repository marketplace registration were removed. Protected runtime state was absent from plugin/package artifacts. A pre-existing Phase-0 MCP fixture process belonging to the active development environment was not modified and is not a v0.4 host/launcher.

Validated claims are stock CLI/custom-client ownership plus plugin MCP ingress. Official desktop/IDE private histories remain typed unsupported targets because no authenticated exclusive owner adapter exists on the pinned build.
